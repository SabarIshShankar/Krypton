<title>Spacesurf</title>
<link href="https://fonts.googleapis.com/css2?family=Montserrat&display=swap" rel="stylesheet">
<link rel="stylesheet" href="https://www.w3schools.com/w3css/4/w3.css">
<style>
	.date{
		font-size: 10px;
		color: rgb(212, 212, 212);
	}
	.chip{
		background-color: #ffffff;
	}
	p,h1, h2, h3, h4, h5, h6, a{
		font-family: Montserrat;

	}
  .pa{
    color: #ee00ff;
  }
	.card-image{
		box-shadow: 0;
	}
    body{
        background-color: rgb(14, 14, 14);
    }
    .w3-bg{
        background-color: #1d1d1d;
        border: 0.6px solid rgb(75, 75, 75);
        border-radius: 10px;
    }
.links{
    color:#ea00ff;
}
          
         .btn-grad {background-image: linear-gradient(to right, #c300ff 0%, #ff00d4  51%, #8c00ff  100%)}
         .btn-grad {
            margin: 10px;
            text-align: center;
            transition: 0.5s;
            background-size: 200% auto;
            color: white;            
            border-radius: 10px;
            display: block;
          }

          .btn-grad:hover {
            background-position: right center;
            color: rgb(255, 255, 255);
            text-decoration: none;
          }
         

</style>

<a class="w3-left btn-grad" href="/stories/add">Create Post</a>
<a class="w3-right btn-grad" href="/dashboard">Profile</a>
<a class="w3-right btn-grad" href="/auth/logout">Logout</a>


<div class="row">
    {{#each stories}}
    <div class="col s12">
        <div class="card w3-bg">
            <div class="card-content left-align w3-text-white">
				<a href="/stories/user/{{user._id}}"><h6>{{user.displayName}}</h6></a>
                <a href="/stories/{{_id}}"><h5>{{title}}</h5></a>
                <p class="pa">{{stripTags (truncate body 60)}}</p>

                <div class="date">
                <p>{{formatDate createdAt 'DD MM YY, h:mm a'}}</p>
                </div>
            </div>
        </div>
    </div>
    {{else}}
    <p class="w3-padding w3-text-white">No posts to display</p>
    {{/each}}
</div>

